integer g, x(g), varia, i
logical temp
data x/5,-3,*2/, temp/2/

if (f .gt. 1) i = 1 + 2
varia = .false.

if ( i .eq. 5 ) then
    if ( f .ne. 0 ) 100, 200, 300
else
    integer l, i, k
    
    do i = 1, l
        k = k + 1
    enddo
endif

stop
end
